---
// 1. Importamos la herramienta mágica de Astro
import { Image } from "astro:assets";

// 2. NUEVA COMBINACIÓN DE FOTOS
import img1 from "../assets/images/Redbull/IMG_1224.jpeg";
import img2 from "../assets/images/Basketball/IMG_3101.jpeg";
import img3 from "../assets/images/Urbano/_DSC3364.jpeg";
import img4 from "../assets/images/Donellz/_DSC3200.jpeg";
import img5 from "../assets/images/Conception/_DSC4296.jpeg";
import img6 from "../assets/images/Beeway/_DSC4792.jpeg";
---

<section class="hero-section">
    <div class="container">
        <div class="carousel-wrapper">
            <button class="carousel-arrow prev">‹</button>

            <div class="carousel-track" id="track">
                <!-- 3. Usamos <Image /> y le pasamos la clase "slide" -->
                <Image
                    class="slide"
                    src={img1}
                    alt="Slide Redbull 2"
                    width={600}
                />
                <Image
                    class="slide"
                    src={img2}
                    alt="Slide Basketball 2"
                    width={600}
                />
                <Image
                    class="slide"
                    src={img3}
                    alt="Slide Urbano 2"
                    width={600}
                />
                <Image
                    class="slide"
                    src={img4}
                    alt="Slide Donellz 2"
                    width={600}
                />
                <Image
                    class="slide"
                    src={img5}
                    alt="Slide Conception 2"
                    width={600}
                />
                <Image
                    class="slide"
                    src={img6}
                    alt="Slide Beeway 2"
                    width={600}
                />
            </div>

            <button class="carousel-arrow next">›</button>
        </div>
    </div>
</section>

<style>
    .hero-section {
        width: 100%;
        margin-top: 2rem;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1rem;
    }

    .carousel-wrapper {
        position: relative;
        width: 100%;
        height: 500px;
        max-height: 65vh;
        overflow: hidden;
    }

    .carousel-track {
        display: flex;
        gap: 10px;
        width: 100%;
        height: 100%;
        overflow-x: hidden;
        scroll-behavior: smooth;
    }

    .slide {
        flex: 0 0 calc(20% - 8px);
        height: 100%;
        object-fit: cover;
        object-position: center;
        /* ELIMINÉ EL FILTRO GRAYSCALE AQUÍ PARA QUE EL ROJO BRILLE */
    }

    .carousel-arrow {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        background: transparent;
        color: var(--white);
        border: none;
        font-size: 3rem;
        cursor: pointer;
        z-index: 10;
        text-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
        transition: transform 0.2s ease;
    }

    .carousel-arrow:hover {
        transform: translateY(-50%) scale(1.2);
    }

    .carousel-arrow.prev {
        left: 10px;
    }
    .carousel-arrow.next {
        right: 10px;
    }

    /* Adaptación a pantallas pequeñas */
    @media (max-width: 1024px) {
        .slide {
            flex: 0 0 calc(33.333% - 7px);
        }
    }

    @media (max-width: 768px) {
        .hero-section {
            margin-top: 1rem;
            margin-bottom: 3rem;
        }
        .carousel-wrapper {
            height: 60vh; /* Un poco más alto para que sea vertical */
            max-height: 450px;
        }
        .slide {
            flex: 0 0 85%; /* Muestra un poco de la siguiente para que se vea que es un carrusel */
            margin-right: 10px;
        }
        .carousel-arrow {
            font-size: 2rem;
        }
        .carousel-arrow.prev {
            left: 10px;
        }
        .carousel-arrow.next {
            right: 10px;
        }
    }
</style>

<script>
    const track = document.getElementById("track");
    const nextBtn = document.querySelector(".carousel-arrow.next");
    const prevBtn = document.querySelector(".carousel-arrow.prev");

    function slideNext() {
        if (!track) return;
        const slide = track.querySelector(".slide");
        if (!slide) return;

        const slideWidth = slide.clientWidth + 10;

        if (track.scrollLeft + track.clientWidth >= track.scrollWidth - 10) {
            track.scrollTo({ left: 0, behavior: "smooth" });
        } else {
            track.scrollBy({ left: slideWidth, behavior: "smooth" });
        }
    }

    function slidePrev() {
        if (!track) return;
        const slide = track.querySelector(".slide");
        if (!slide) return;

        const slideWidth = slide.clientWidth + 10;
        track.scrollBy({ left: -slideWidth, behavior: "smooth" });
    }

    let autoPlay = setInterval(slideNext, 3000);

    function resetAutoPlay() {
        clearInterval(autoPlay);
        autoPlay = setInterval(slideNext, 3000);
    }

    if (nextBtn && prevBtn && track) {
        nextBtn.addEventListener("click", () => {
            slideNext();
            resetAutoPlay();
        });

        prevBtn.addEventListener("click", () => {
            slidePrev();
            resetAutoPlay();
        });
    }
</script>
